-- htmlgeom is the html geometry object used to generate element/cell information for layout_changed
-----------------------------------------------------------------------------------------------------------------------------------

local tinsert 		= table.insert 
local tremove 		= table.remove 

local events 		= {}

-----------------------------------------------------------------------------------------------------------------------------------
-- Rgister geometry for an event (like click, enter, exit etc)
--
--   Geometry objects are the equivalent of a "div" in html5
local function registergeom( g, geom )


end

-----------------------------------------------------------------------------------------------------------------------------------
-- Query is intended as an equivalent of a JQuery call. 
--    To ensure ease of use, I use the same style of constructs for events.
--      html.query(".myclass").on( "pressed", function(event) .... end )
--  or  html.query("#myid").on( "released", function(event) .... end )
local function query( selector )

	
	
end

-----------------------------------------------------------------------------------------------------------------------------------
local function addelement( info )

end 

-----------------------------------------------------------------------------------------------------------------------------------

local function updateelements()

end

-----------------------------------------------------------------------------------------------------------------------------------

return events

-----------------------------------------------------------------------------------------------------------------------------------
